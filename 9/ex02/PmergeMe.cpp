#include "PmergeMe.hpp"

/*
#include <vector>
#include <iostream>
#include <algorithm>

void testSort()
{
    std::vector<int> vec =  { 5, 9, 8, 1, 0, 4, 2, 1 };

    insertionSort(vec, 0, 3);
    insertionSort(vec, 4, vec.size() - 1);

    std::vector<int> vec2 = { 1, 5, 8, 9, 0, 1, 2, 4 };

    assert( vec == vec2 );

    vec = { 5, 9, 8, 1, 0, 4, 2, 1 };

    mergeSort(vec, 0, vec.size() - 1);
    vec2 = { 0, 1, 1, 2, 4, 5, 8, 9 };

    assert( vec == vec2 );

    vec = { 5, 9, 24, 23, 25, 8, 1, 0, 11, 4, 2, 1, 20 };

    FordJohnsonSort(vec, 0, vec.size() - 1);
    vec2 = { 0, 1, 1, 2, 4, 5, 8, 9, 11, 20, 23, 24, 25 };

    assert( vec == vec2 );
}

*/
